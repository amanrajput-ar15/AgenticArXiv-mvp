[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port ${PORT}"
healthcheckPath = "/health"
healthcheckTimeout = 100

[[mounts]]
source = "agenticarxiv-data"
destination = "/mnt/volume"